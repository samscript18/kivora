"use client";
import { usePrivy } from "@privy-io/react-auth";
import { ArrowRight, LogOut, ShieldCheck } from "lucide-react";
import Link from "next/link";
import { Dashboard } from "@/components/dashboard/Dashboard";

function Gate(){const {ready,authenticated,login,logout}=usePrivy();if(!ready)return <div className="grid min-h-screen place-items-center font-mono text-xs uppercase tracking-widest text-slate-500">Verifying Privy session…</div>;if(!authenticated)return <div className="grid min-h-screen place-items-center p-5"><div className="glass-card max-w-md rounded-3xl p-8 text-center"><div className="mx-auto grid h-12 w-12 place-items-center rounded-2xl border border-[#FF1301]/30 bg-[#FF1301]/10 text-accent"><ShieldCheck/></div><h1 className="mt-6 font-display text-3xl font-black">Your revenue workspace is protected.</h1><p className="mt-3 text-sm leading-6 text-slate-400">Sign in with Privy to load your live portfolio and authorize revenue actions.</p><button onClick={login} className="mx-auto mt-7 flex items-center gap-2 rounded-full bg-gradient-to-r from-accent to-accent-2 px-7 py-3.5 text-sm font-bold">Sign in with Privy <ArrowRight size={15}/></button><Link href="/" className="mt-5 block text-xs text-slate-500">Return home</Link></div></div>;return <><button onClick={logout} className="fixed bottom-5 left-5 z-[70] flex items-center gap-2 rounded-xl border border-white/10 bg-surface/90 px-4 py-2.5 font-mono text-[10px] uppercase tracking-wider text-slate-300 backdrop-blur"><LogOut size={13}/> Sign out</button><Dashboard/></>}
export default function DashboardPage(){if(!process.env.NEXT_PUBLIC_PRIVY_APP_ID)return <div className="grid min-h-screen place-items-center p-5"><div className="glass-card max-w-lg rounded-3xl p-8"><h1 className="font-display text-2xl font-black">Privy configuration required</h1><p className="mt-3 text-sm text-slate-400">Add <code className="text-accent">NEXT_PUBLIC_PRIVY_APP_ID</code> to <code>frontend/.env.local</code>, then restart Next.js.</p></div></div>;return <Gate/>}
